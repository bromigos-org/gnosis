# Run 3 — relevance ranking + compact rendering (gnosis PR #7)
# LOCOMO subset-3 context J: 59.5.
# CODE change (ranked facts reusing the vector path + compact rendering); no flags.
# Loads onto current gnosis code (flags only; CODE-level changes are in main).

# (no feature flags — all default off)
